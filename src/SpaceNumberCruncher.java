import java.util.Scanner;

public class SpaceNumberCruncher {
    /* static double[] convertToDoubleArray(String data)
       Split data at the commas. For each piece, try to convert it to a double.
       If a piece can't be converted (bad format, letters, empty, etc.), use 0.0
       for that position instead of skipping it - the array should have one
       element for every comma-separated piece.
       If data itself is empty or blank, return an array with zero elements.
    */


    /* static double calculateAverage(double[] measurements)
       Return the average of all the values in measurements (total / count),
       or 0.0 if measurements has zero elements.
    */


    /* static double findHighest(double[] measurements)
       Return the highest value in measurements.
    */


    /* static double findLowest(double[] measurements)
       Return the lowest value in measurements.
    */


    // Main method
    public static void main(String[] args) {
        // Create a Scanner
        // Prompt: "Enter space probe data (comma-separated numeric values): "
        // Read the line of input

        // Convert the input to a double array using convertToDoubleArray

        // If the array has zero elements, display:
        // "Invalid input. Please provide numeric values."
        // and stop.

        // Otherwise, determine the average, highest, and lowest.

        // Display:
        // "Space Probe Data Statistics:"
        // "Average measurement: [average]"
        // "Highest measurement: [highest]"
        // "Lowest measurement: [lowest]"
    }
}
