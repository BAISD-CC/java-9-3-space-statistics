import java.util.Scanner;
public class SpaceNumberCruncher {
    /* The static double array returning method convertToDoubleArray accepts an input.
       It should split the input at commas and verify that the input for each value is valid.
       If the input is good, it should be included in a double array, otherwise, the value
       should be 0.0 and still be included. The double array should be returned.
    */

    /* The static double returning array calculateAverage should accept a double 
       array as a parameter. It should return the average for all of the values 
       (total/number of elements) or 0.0 if the array has 0 elements.
    */

    /* The static double returning method findHighest acccepts a double array as 
       a parameter. It should determine the highest number in the array and return it.
    */

    /* The static double returning method findLowest accepts a double array as a parameter.
       It should determind the lowest number in the array and return it.
    */


    // Main method
    public static void main(String[] args) {
        // Scanner
        // Ask: "Enter space probe data (comma-separated numeric values): "
        // Get input

        // Convert to double array

        // Verify the length of the array is longer than 0. 
        // If it isn't display: "Invalid input. Please provide numeric values."

        // Determine the average.
        // Determine the highest.
        // Determine the lowest.

        // Display: 
        // "Space Probe Data Statistics:"
        // "Average measurement: [average]"
        // "Highest measurement: [highest]"
        // "Lowest measurement: [lowest]"
    }
}
